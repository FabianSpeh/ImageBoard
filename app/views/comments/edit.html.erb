<!--<%= render 'edit_form', comment: @comment%>--->

<div class="comment-form">
<%= form_for @comment, url: post_comment_path(post_id: @comment.post_id, id: @comment.id) do |f| %>
<% f.label 'Comment Body' %><br>
<%= f.text_area :body %>

<%= f.submit "Change Comment"%>
<%end%>
<%= link_to 'Back', posts_path(@comment.post_id) %>
</div>